name: Publish @clypra/feature-providers

on:
  push:
    branches:
      - main
    paths:
      - "packages/feature-providers/src/**"
      - "packages/feature-providers/package.json"
      - "packages/feature-providers/tsup.config.ts"
      - "packages/feature-providers/tsconfig.json"
      - "packages/feature-providers/README.md"
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Install monorepo dependencies
        run: npm install --workspaces --include-workspace-root

      - name: Build @clypra/feature-providers
        run: npm run build:feature-providers

      - name: Run semantic-release
        working-directory: packages/feature-providers
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GIT_AUTHOR_NAME: "github-actions[bot]"
          GIT_AUTHOR_EMAIL: "github-actions[bot]@users.noreply.github.com"
          GIT_COMMITTER_NAME: "github-actions[bot]"
          GIT_COMMITTER_EMAIL: "github-actions[bot]@users.noreply.github.com"
