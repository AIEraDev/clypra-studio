name: Publish @clypra/engine

on:
  push:
    branches:
      - main
    paths:
      # Only trigger when engine source, config, or docs actually change
      - "packages/clypra-engine/src/**"
      - "packages/clypra-engine/package.json"
      - "packages/clypra-engine/tsup.config.ts"
      - "packages/clypra-engine/tsconfig.json"
      - "packages/clypra-engine/README.md"
  workflow_dispatch: # Allow manual trigger from GitHub UI

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Install monorepo dependencies
        run: npm ci --workspaces --include-workspace-root

      - name: Build dependencies first
        run: |
          npm run build:types
          npm run build:shader-library
          npm run build:runtime

      - name: Build @clypra/engine
        run: npm run build:engine

      - name: Run semantic-release
        working-directory: packages/clypra-engine
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GIT_AUTHOR_NAME: "github-actions[bot]"
          GIT_AUTHOR_EMAIL: "github-actions[bot]@users.noreply.github.com"
          GIT_COMMITTER_NAME: "github-actions[bot]"
          GIT_COMMITTER_EMAIL: "github-actions[bot]@users.noreply.github.com"
