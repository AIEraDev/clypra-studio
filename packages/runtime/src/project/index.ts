/**
 * @clypra/runtime — Project Model
 *
 * Defines immutable project manifests, clips, tracks, and asset handles for the V2 pipeline.
 * This module handles the NLE (Non-Linear Editor) project structure that gets compiled
 * into MediaProcessingGraphs.
 */

export * from "./types";
